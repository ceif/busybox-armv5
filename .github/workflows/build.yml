name: Build BusyBox ARMv5 AK3918 Stable

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - name: Install toolchain
        run: |
          sudo apt update
          sudo apt install -y gcc-arm-linux-gnueabi make wget bzip2

      - name: Download BusyBox
        run: |
          wget https://busybox.net/downloads/busybox-1.32.1.tar.bz2
          tar -xjf busybox-1.32.1.tar.bz2

      - name: Configure
        run: |
          cd busybox-1.32.1
          make ARCH=arm CROSS_COMPILE=arm-linux-gnueabi- defconfig
          sed -i 's/^# CONFIG_STATIC is not set/CONFIG_STATIC=y/' .config
          sed -i 's/^CONFIG_PIE=y/# CONFIG_PIE is not set/' .config
          echo 'CONFIG_EXTRA_CFLAGS="-march=armv5te -mtune=arm926ej-s -marm -msoft-float -mno-unaligned-access -fno-tree-vectorize -O1"' >> .config

      - name: Build
        run: |
          cd busybox-1.32.1
          make ARCH=arm CROSS_COMPILE=arm-linux-gnueabi- -j$(nproc)

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: busybox-armv5-ak3918
          path: busybox-1.32.1/busybox
